export function verifyWebhook(): boolean {
  // TODO(Fluid Pay docs): verify webhook signature header name.
  // TODO(Fluid Pay docs): verify webhook timestamp header name, if any.
  // TODO(Fluid Pay docs): verify HMAC/signature payload construction and replay tolerance.
  return false
}
